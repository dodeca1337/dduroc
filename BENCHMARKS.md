# Замеры

`cargo bench -p dduroc --bench hot_path`

Числа сняты на x86-64 (Linux, `/dev/shm`, сборка release). На armv7 они
будут в несколько раз хуже в абсолютном выражении, но **соотношения между
вариантами переносятся** — именно ради них замеры и существуют.

Хранилище пишется в память, а долговечность в конфигурации бенчмарка
отключена: цель — измерить стоимость собственного кода, а не скорость
носителя разработчика. Стоимость `fdatasync` на eMMC (1–10 мс) от кода не
зависит, зато зависит от того, сколько раз он вызван, — это проверяется
тестом `critical_burst_is_one_group_commit`.

## Кодеки формата

| Операция | Время | Пропускная способность |
|---|---|---|
| varint, запись малого значения | 1.16 нс | 858 М/с |
| varint, чтение | 4.90 нс | 204 М/с |
| кодирование сообщения | 8.00 нс | 125 М/с |
| декодирование сообщения | 17.9 нс | 56 М/с |
| кодирование сэмпла | 7.37 нс | 136 М/с |
| сериализация полей (1 поле) | 11.4 нс | — |
| сериализация полей (2 поля) | 22.7 нс | — |

## Блоки

| Операция | На 1000 записей | На запись |
|---|---|---|
| сборка блока без сжатия | 17.3 мкс | 17.3 нс |
| сборка блока с LZ4 | 13.6 мкс | 13.6 нс |
| разбор и обход блока | 30.9 мкс | 30.9 нс |

LZ4 быстрее «без сжатия»: тело копируется в выходной буфер уже сжатым,
и на хорошо сжимаемых логах копировать приходится меньше.

## Запись

Два режима, и их важно не путать.

**Ненасыщённый** — то, что видит приложение в реальной работе: очередь
пуста, writer успевает.

| Операция | На запись |
|---|---|
| событие (пачка 1000) | 297 нс |
| сэмпл телеметрии (пачка 1000) | 53 нс |

Разрыв между ними — не сериализация (она 11–23 нс), а пробуждение
writer-потока: при редком потоке событий каждая постановка в очередь
будит спящего потребителя.

**Насыщённый** — криterion гонит вызовы плотнее, чем writer разбирает
очередь, поэтому измеряется смесь удачных постановок и быстрых отказов
при переполнении. Числа полезны только для сравнения вариантов между
собой: `write/log/simple`, `write/log/critical` и т.д.

## Сквозная пропускная способность

| Сценарий | Время | Пропускная способность |
|---|---|---|
| 10 000 событий с открытием хранилища и синхронизацией | 2.37 мс | 4.2 М событий/с |

## Масштаб

| Сценарий | Время | На неймспейс |
|---|---|---|
| подъём 10 неймспейсов | 1.64 мс | 164 мкс |
| подъём 100 неймспейсов | 14.7 мс | 147 мкс |

Линейно. При заявленном пределе в 24 тысячи неймспейсов старт займёт
около 3.5 с — приемлемо, но это первый кандидат на оптимизацию, если
предел будет выбираться целиком. Пустой неймспейс не создаёт сегмента и
не занимает ни файлового дескриптора, ни преаллоцированных байт.

## Чтение

Хранилище: 100 000 сообщений в 4 неймспейсах.

| Запрос | Время | Пропускная способность |
|---|---|---|
| все записи | 17.8 мс | 5.6 М записей/с |
| последние 100 | 1.88 мс | — |
| только ошибки (их нет) | 3.90 мс | — |

## Что дали оптимизации

Замеры до и после исправлений, найденных бенчмарками и аудитом:

| Показатель | Было | Стало | Выигрыш |
|---|---|---|---|
| чтение всех 100k записей | 531 мс | 17.8 мс | **30×** |
| выборка только ошибок | 506 мс | 3.9 мс | **130×** |
| последние 100 записей | 1.24 мс | 1.88 мс | −34% ¹ |
| критическое событие (насыщ.) | 257 нс | 145 нс | −53% |
| сэмпл телеметрии (насыщ.) | 61 нс | 48 нс | −21% |

¹ Замедление — плата за корректность: идентичность серий телеметрии
теперь восстанавливается по таблице из footer'а, а не по потоку записей,
в котором при обратном обходе определения серий встречаются уже после
своих сэмплов.

Что именно было исправлено:

- **Резолв схемы читал `ns-meta` с диска на каждую запись** — файловая
  операция на запись. Схема разрешается один раз на неймспейс.
- **Отфильтрованные записи материализовались** — аллокация payload'а на
  каждую из сотен тысяч отброшенных. Предикат применяется до владеющей
  копии.
- **Имена неймспейса и канала копировались в каждую запись ответа** —
  две аллокации на запись, заменены на `Arc<str>`.
- **Логирование аллоцировало промежуточный `Vec`** — postcard пишет прямо
  в буфер, который уйдёт в очередь.
- **Учёт потерь шёл через мьютекс и хеш-таблицу** — на горячем пути ровно
  тогда, когда система под давлением. Заменён массивом атомиков.
- **Поиск дескриптора по схеме был линейным** — теперь бинарный.
- **Обход всех каналов на каждом обороте цикла writer'а** — заменён
  списком активных: при 24 тысячах неймспейсов полный проход съедал бы
  процессор впустую, тогда как пишущих в любой момент единицы.
